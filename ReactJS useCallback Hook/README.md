# React useCallback Hook

`useCallback` is a React Hook that lets you cache a function definition between re-renders. It is primarily used for performance optimization when passing callbacks to optimized child components.

## Concepts Covered
- Preventing function recreation on every render.
- Working in conjunction with `React.memo` to optimize child component renders.
- Walkthrough via a **TaskList** and **TaskItem** list rendering system.
